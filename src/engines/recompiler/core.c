#include<stdio.h>

// The main idea behind recompiling is:
// getopcode
// see if cached
// if not, cache
// else use the cache

int run_engine(int argc, char const *argv[]) {
    printf("Hello 2\n");
    
    return 0;
}
